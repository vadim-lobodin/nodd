# Always resolve against public npm. A previous lockfile was generated behind an
# internal mirror, which baked private URLs into every "resolved" field and broke
# installs for anyone without credentials to that mirror.
registry=https://registry.npmjs.org/
